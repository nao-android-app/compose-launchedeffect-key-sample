package com.nao.launchedeffectkeysample

enum class KeyType(
    val displayName: String,
) {
    UNIT("Unit"),
    TRUE("true"),
    COUNT("Count"),
    TEXT("Text"),
    COUNT_AND_TEXT("Count + Text"),
}
