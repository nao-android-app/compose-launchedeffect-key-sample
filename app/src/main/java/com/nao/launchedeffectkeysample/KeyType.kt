package com.nao.launchedeffectkeysample

enum class KeyType(
    val label: String,
) {
    UNIT("Unit"),
    TRUE("true"),
    COUNT("Count"),
    TEXT("Text"),
    COUNT_AND_TEXT("Count + Text"),
}
